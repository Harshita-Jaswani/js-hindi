const accountId="hello";
let Name="harshita";
var number=1234;
let state;// this will be undefined.

rollno = 20; // this is also possible ,
 //but we should avoid this method.

/* const cant be changed.
 also let and var both are same, but we should use let to avoid scope issues.
*/
console.log(number)
console.table([Name,number,state,rollno])